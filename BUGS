Известные ошибки:

* Команда "Вид->Показать с адреса" а потом покрутить прокрутку- переходит к показу предыдущей позиции.

* вводим мнемонику LDA 0b00 - в памяти видим LDA 0000. LDA 0x0b00 работает правильно.
  Expected result: должно выдавать ошибку: 0b00 не является десятичным числом (шестнадцатиричные числа должны начинаться с 0x)

* В справке про директивы ассемблера DB написан с шестнадцатиричными константами без 0x в начале

* В справке упомянуты файлы Bugs.txt ToDo.txt но на самом деле они называются не так.
  TODO: переименовать в BUGS.txt и TODO.txt

+ Если нажали на StepOver, а он до следующей строки не дошел (пришлось нажать Стоп), 
то временную точку останова нужно снять!

